# Surface Code Simulator Configuration
# Lines starting with # are comments

# Code parameters
code_distance 5
rounds 5
code_type rotated_memory_x

# Error rates (as fractions, e.g., 0.001 = 0.1%)
physical_error 0.001
measurement_error 0.001
reset_error 0.001

# Number of shots to simulate
# Supports magnitude suffixes: K (thousands), M (millions), B (billions), G (billions)
total_shots 10M
